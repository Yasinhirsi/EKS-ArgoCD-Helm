provider: aws

env: 
  - name: AWS_DEFAULT_REGION
    value: eu-west-2

serviceAccount:
  create: true
  name: external-dns
  annotations: {} #tf will inject from irsa.yml from set block

domainFilters:
  - eks.yasinhirsi.com